{
  "name": "dgmascotas",
  "version": "1.0.0",
  "description": "Sistema veterinario DG Mascotas - Full Stack",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:all": "npm run build --prefix frontend",
    "test:all": "npm test --prefix backend",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:build": "docker-compose build --no-cache",
    "docker:logs": "docker-compose logs -f",
    "db:reset": "docker-compose exec dgmascotas-db psql -U postgres -d dgmascotas_db -c \"DROP SCHEMA public CASCADE; CREATE SCHEMA public;\"",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
